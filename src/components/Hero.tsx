import React from 'react';
import { makeStyles } from '@material-ui/core'

import './Hero.css';

// const useStyles = makeStyles({
// 	background: {
// 		height: '100vh',
// 		width: '100%',
// 		background: `
// 			url(${beakersImage}), 
// 			linear-gradient(#006, #555)`,
// 	}
// })

const Hero = () => {
	// const classes = useStyles({})
	return (
		<div className={'background'}>

		</div>
	)
}

export default Hero
